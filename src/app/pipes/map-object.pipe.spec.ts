import { MapObjectPipe } from './map-object.pipe';

describe('MapObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new MapObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
